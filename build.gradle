allprojects {
    apply plugin: 'idea'
}

subprojects {
    apply plugin: 'java'

    group 'jsense'
    status 'release'

    sourceCompatibility = '1.6'
    targetCompatibility = '1.6'

    repositories {
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.8'
}